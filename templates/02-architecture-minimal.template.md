# Architecture Rules (Uninitialized)

> ⚠️ **Project Context Not Initialized**

Hiện tại chưa có Architecture Rules cụ thể cho dự án này.

## Action Required

Vui lòng chạy lệnh sau để khởi tạo ngữ cảnh và sinh bộ quy tắc phù hợp với Tech Stack của bạn:

```bash
/setup
```

Quy trình Setup sẽ:
1.  Nhận diện Tech Stack (Flutter, Node.js, Python...).
2.  Phân tích Source Code (nếu có) hoặc Requirements (nếu mới).
3.  Tự động generate `ARCHITECTURE.md` chuẩn xác nhất.
