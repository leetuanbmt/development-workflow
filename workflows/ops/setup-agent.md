---
description: "Tự động quét dự án và cập nhật file cấu hình ngữ cảnh (Auto-Discovery)."
trigger: /setup-agent
version: "2.4.0"
skills:
  - devops-engineer
---

# 🛠 Setup Agent Context (Auto-Discovery)

**Mục tiêu:** Tự động hóa việc cài đặt ngữ cảnh cho Agent khi chuyển sang dự án mới. Thay thế việc cấu hình thủ công.

## 🚀 Các bước thực hiện (Execution Steps)

0.  **Chọn thư mục dự án (Select Project Directory):**
    *   **HÀNH ĐỘNG:** Chạy script `development-workflow/scripts/select_project_dir.sh` để xác định thư mục cần quét.
    *   Nếu script không chạy được trong môi trường hiện tại, hãy hỏi người dùng trực tiếp trong chat: "Bạn muốn quét từ root hay một thư mục con cụ thể?"
    *   Lưu đường dẫn thư mục đã chọn (ví dụ: `SELECTED_DIR`) để sử dụng cho các bước tiếp theo.

1.  **Quét File Cấu Hình (Scan Signature Files):**
    *   Tìm và đọc các file sau trong `SELECTED_DIR` (nếu tồn tại): `pubspec.yaml`, `package.json`, `Makefile`, `README.md`, `.gitignore`, `melos.yaml`.
    *   Đọc `SELECTED_DIR/lib/main.dart` (hoặc entry point tương đương) để đoán kiến trúc sơ bộ.

2.  **Phân tích & Trích xuất (Analyze & Extract):**
    Dựa trên nội dung đọc được, hãy xác định:
    *   **Project Name & Description.**
    *   **Core Tech Stack:**
        *   Language/Framework (VD: Flutter 3.x, React 18).
        *   State Management (VD: Bloc, Riverpod, Redux).
        *   Navigation (VD: AutoRoute, GoRouter).
        *   Network (VD: Dio, Retrofit, Axios).
        *   Database (VD: Drift, Hive, Room).
    *   **Development Commands:**
        *   Tìm lệnh Build, Run, Test, Gen code trong `Makefile` hoặc `scripts` của `package.json`.
    *   **CI/CD & Automation Discovery:**
        *   Tìm thư mục `.github/workflows` (GitHub Actions).
        *   Tìm `.gitlab-ci.yml` (GitLab CI).
        *   Tìm `fastlane/` (Fastlane automation).
        *   Tìm `Jenkinsfile` (Jenkins).
        *   Tìm `appcenter-post-clone.sh` (AppCenter).
    *   **Environment Versions:**
        *   Đọc `android/build.gradle` hoặc `android/app/build.gradle` để tìm `minSdkVersion`, `targetSdkVersion`, `compileSdkVersion`.
        *   Đọc `android/gradle/wrapper/gradle-wrapper.properties` để tìm Gradle version.
    *   **Design System Discovery:**
        *   Tìm các file có tên chứa `color`, `theme`, `typography`, `style` trong `lib/core` hoặc `lib/shared`.
        *   Ghi chú lại đường dẫn các file này để Designer AI tham khảo sau này.
    *   **Project Structure:**
        *   Kiểm tra xem có dùng Clean Architecture (`data/domain/presentation`) hay MVC không.

3.  **Cập nhật Cấu hình (Configuration Update):**
    *   **HÀNH ĐỘNG:** Ghi đè (Overwrite) toàn bộ nội dung file `.gemini/rules/01-project-context.md` (hoặc `.agent/memory/PROJECT.md`) theo mẫu chuẩn bên dưới, thay thế các placeholder bằng dữ liệu thực tế vừa quét được.
4.  **Đồng bộ & Kiểm tra (Sync & Verify):**
    *   **HÀNH ĐỘNG:** Chạy lệnh `make sync` (hoặc `bash development-workflow/scripts/sync.sh`) để đồng bộ ngữ cảnh sang `.agent/` và `.gemini/`.
    *   **KIỂM TRA:** Đảm bảo file `.agent/memory/PROJECT.md` đã được cập nhật nội dung mới nhất.
## 📝 Template chuẩn cho `01-project-context.md` (Output Target)

```markdown
---
trigger: always_on
description: Ngữ cảnh kỹ thuật tự động của dự án [Project Name].
---

# 🌍 Project Context: [Project Name]

> **Last Updated:** [Date]
> **Auto-generated by:** `/setup-agent`

## 1. Tổng quan (Overview)
*   **Tên dự án:** [Project Name]
*   **Mô tả:** [Description from pubspec/README]
*   **Ngôn ngữ chính:** [Language & Version]

## 2. Tech Stack (Core Libraries)
*   **Framework:** [Flutter/React/...]
*   **State Management:** [Bloc/Provider/...]
*   **Architecture:** [Clean Arch/MVC/...]
*   **Dependency Injection:** [GetIt/Injectable/...]
*   **Networking:** [Dio/Retrofit/...]
*   **Local DB:** [Drift/Hive/...]
*   **Navigation:** [AutoRoute/GoRouter/...]

## 3. Development Commands (Lệnh thường dùng)
*   **Setup:** `[command]` (VD: make setup)
*   **Run Dev:** `[command]`
*   **Code Gen:** `[command]`
*   **Test:** `[command]`
*   **Lint:** `[command]`
*   **Build:** `[command]`

## 4. Structure Guidelines
*   **Entry Point:** `[path/to/main]`
*   **Feature Folder:** `[path/to/features]`
```

## ⚠️ Lưu ý
*   Nếu không tìm thấy file Makefile, hãy tìm lệnh trong `package.json` hoặc gợi ý các lệnh CLI chuẩn (VD: `flutter run`).
*   Giữ nguyên các quy tắc về Clean Architecture nếu phát hiện cấu trúc thư mục tương ứng.
