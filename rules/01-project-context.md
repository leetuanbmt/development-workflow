---
trigger: always_on
description: Ngữ cảnh kỹ thuật của dự án Development Workflow.
---

# 🌍 Project Context: Gemini CLI Development Workflow

> **Last Updated:** 2026-01-30
> **Auto-generated by:** `/setup`

## 1. Tổng quan (Overview)
*   **Tên dự án:** Gemini CLI Development Workflow (Kansuke Edition)
*   **Mô tả:** Bộ quy trình, kỹ năng và quy tắc chuẩn hóa để AI Agent (Gemini) vận hành theo mô hình Auditor-First.
*   **Ngôn ngữ chính:** Python 3.x, Bash Shell.

## 2. Tech Stack (Core Libraries)
*   **Framework:** Gemini CLI Extension System.
*   **Configuration:** Markdown (Docs), TOML (Commands), YAML (Metadata).
*   **Automation:** Bash Scripts (`scripts/*.sh`), Python Scripts (`scripts/*.py`).
*   **Architecture:** Modular Skill-Based Architecture (Rules + Skills + Workflows).

## 3. Development Commands (Lệnh thường dùng)
*   **Sync Environment:** `./scripts/sync.sh` (Đồng bộ changes vào `.gemini/`)
*   **Validate:** `./scripts/validate.sh` (Kiểm tra lỗi cú pháp MD/TOML)
*   **Test Scripts:** `python3 -m unittest discover tests`
*   **Lint Python:** `flake8 scripts/`

## 4. Structure Guidelines
*   **Source Root:** `development-workflow/` (Hiện tại là root)
*   **Rules:** `rules/` (Nguyên tắc cốt lõi)
*   **Skills:** `skills/` (Khả năng chuyên môn)
*   **Workflows:** `workflows/` (Quy trình tương tác)
*   **Generated Runtime:** `.gemini/` (Không sửa trực tiếp ở đây)